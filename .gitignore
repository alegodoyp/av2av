
# Credentials and Secrets
client_secrets.json
google_credentials.json
token.json
token.pickle
*.json
!package.json
# Not a credential -- required config for load_unit2av_model(), and small (~1KB).
!unit2av/config.json

# Environment
venv/
.env

# Data and Checkpoints
checkpoints/
data/
videos_mestrado/
synthetic_targets/
temp_synthetic_gen/
test_videos_result/
test_videos_tempo/
test_data/
result_data/

# Python
__pycache__/
*.pyc
*.pt
*.pth
# Speaker-encoder checkpoint, small enough for plain git (~17MB, well under
# GitHub's 100MB hard limit) -- everything else under checkpoints/ and
# latentsync_repo/checkpoints/ is multi-hundred-MB to multi-GB and must stay
# out of git; transfer those via scp instead (see conversation).
!unit2av/encoder.pt
